import React, {userState, useEffect} from 'react'
import Header from '../Header/Header'

const Profile = () => {
    return (
        <section>
           <Header />
        </section>
    )
}

export default Profile